spring:
  application:
    name: service-center-fegin
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 9002
# feign配置
feign:
  hystrix:
    enabled: true